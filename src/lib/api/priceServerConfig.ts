/**
 * Base URL of the pykrx/yfinance price server (see price-server/). Update this
 * once the service is deployed (Render.com Blueprint deploy, see price-server/README.md).
 */
export const PRICE_SERVER_BASE_URL = 'https://REPLACE-WITH-RENDER-URL.onrender.com'
